/* tslint:disable:no-unused-variable *//*
import { async, ComponentFixture, TestBed, inject } from '@angular/core/testing';
import { By } from '@angular/platform-browser';
import { DebugElement } from '@angular/core';

import { GalleryParentComponent } from './gallery-parent.component';
import { GalleryComponent } from './gallery.component';
import { ViewerComponent } from './viewer.component';

describe('GalleryParentComponent (inline template)', () => {
  let component: GalleryParentComponent;
  let fixture: ComponentFixture<GalleryParentComponent>;

  beforeEach(() => {
    TestBed.configureTestingModule({
      declarations: [ GalleryParentComponent, GalleryComponent, ViewerComponent ]
    })
    .compileComponents();
  });

  beforeEach(() => {
    fixture = TestBed.createComponent(GalleryParentComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create component', () => {
    inject([GalleryParentComponent, GalleryComponent, ViewerComponent], (app: GalleryParentComponent, g: GalleryComponent, v: ViewerComponent) => {
      expect(component).toBeTruthy();
    }) 
  });
});
*/